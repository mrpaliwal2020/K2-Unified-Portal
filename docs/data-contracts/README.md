# K2 Portal Data Contracts

Document the language-neutral API contracts consumed by the portal: endpoint,
request/response examples, authorization expectation, ownership, error shape,
pagination, compatibility, and affected mobile/portal clients.

The backend is the contract owner. A portal document records the client view; it
does not replace backend API documentation.
