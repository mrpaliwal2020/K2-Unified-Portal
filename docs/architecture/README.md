# K2 Portal Architecture

Document React application boundaries, routes, authorization UX, API client and
module ownership, state ownership, configuration, build/deployment, and
third-party dependencies. Keep portal decisions separate from mobile and
backend implementation details.
